---
interface Props {
  from: string;
  to: string;
}
const { from, to } = Astro.props;
---

<div
  class="section-divider"
  style={`background: linear-gradient(to bottom, ${from}, ${to})`}
  aria-hidden="true"
/>
